# 1. Update package list
sudo apt-get update

# 2. Install Java 21 JDK
sudo apt-get install openjdk-21-jdk -y

# 3. Verify Version (MUST output "21.x")
java -version

# 4. Install Maven (for building your own modules later)
sudo apt-get install maven -y

  # 1. Create project directory
mkdir ~/sdn-project && cd ~/sdn-project

# 2. Download ODL Titanium (Checking for latest Service Release)
# Note: Links change slightly per release. This is the standard path structure.
wget https://nexus.opendaylight.org/content/repositories/opendaylight.release/org/opendaylight/integration/opendaylight/0.22.1/opendaylight-0.22.1.tar.gz

# 3. Extract
tar -xvf opendaylight-0.22.1.tar.gz

# 4. Enter directory
cd opendaylight-0.22.1

# 5. Start Karaf (The Controller Shell)
./bin/karaf

Deployment Steps

Controller Version: OpenDaylight Titanium (Java 21).

Base OS: Linux (Ubuntu 22.04 LTS).

Key Features Installed:

odl-restconf (Northbound API)

odl-netconf-topology (Southbound Device Connection)

odl-netconf-connector-all (Connectors for various device types)

odl-restconf-openapi (The GUI/Documentation Interface)

Credentials: admin / admin

Management URL: http://<IP>:8181/openapi/explorer/index.html
